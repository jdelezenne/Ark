#pragma once

#include <algorithm>
#include <utility>

namespace Ark
{
    using std::exchange;
    using std::forward;
    using std::max;
    using std::min;
    using std::move;
    using std::swap;
}
