#pragma once

namespace Ark
{
    /// The casing of a character or string.
    enum class Casing
    {
        Lowercase, ///< Lowercase form.
        Uppercase, ///< Uppercase form.
    };

    struct Character;

    struct String;
    struct StringSlice;
}
